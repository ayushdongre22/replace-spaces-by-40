#include <bits/stdc++.h> 
string replaceSpaces(string &str){
	string s="";
	for(int i=0;i<str.size();i++){
		if(str[i]!=' '){s.push_back(str[i]);}
		else{s.push_back('@');
		s.push_back('4');
		s.push_back('0');}
	}
	return s;
}
